name: "Run All Tests of YOCKOW's Swift Packages."
on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
    paths:
      - '**/*.swift'
      - '**/*.c'
      - '**/*.h'
      - '.github/workflows/*.yml'
      - 'run-tests'
  pull_request:
    paths:
      - '**/*.swift'
      - '**/*.c'
      - '**/*.h'
      - '.github/workflows/*.yml'
jobs:
  test:
    defaults:
      run:
        working-directory: '.'
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macOS-26
        swift-version:
          - '6.3.1'
          - '6.2.4'
        swift-compat-ver:
          - '6'
          - '5'
          # - '4.2'
          # - '4'
    runs-on: ${{ matrix.os }}
    steps:
    - uses: ikalnytskyi/action-setup-postgres@v8
      with:
        username: swiftpq_test
        password: swiftpq_test
        database: swiftpq_test
    - name: Install depencencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install --no-install-recommends zsh libcurl4-openssl-dev libecpg-dev libpgtypes3
    - uses: actions/checkout@v6
    - name: Cache scratch directory
      uses: actions/cache@v5
      with:
        path: .build
        key: build-${{ github.workspace }}-${{ runner.os }}-${{ matrix.os }}-${{ matrix.swift-version }}-${{ matrix.swift-compat-ver }}-${{ hashFiles('run-tests') }}
        restore-keys: |
          build-${{ github.workspace }}-${{ runner.os }}-${{ matrix.os }}-${{ matrix.swift-version }}-${{ matrix.swift-compat-ver }}-
          build-${{ github.workspace }}-${{ runner.os }}-${{ matrix.os }}-${{ matrix.swift-version }}-
          build-${{ github.workspace }}-${{ runner.os }}-${{ matrix.os }}-
          build-${{ github.workspace }}-${{ runner.os }}-
          build-${{ github.workspace }}-
    - uses: YOCKOW/Action-setup-swift@v1
      with:
        swift-version: ${{ matrix.swift-version }}
    - name: "Execute `run-tests`"
      run: ./run-tests --swift-version ${{ matrix.swift-compat-ver }} --exclude CSV.swift
